// type contextData<TPayload, TResponse, TError> = {
//   data?: TResponse;
//   payload?: TPayload;
//   error?: TError;
// };

// type ServicesType<R> = { fetchData: { data: R } };

// type EventsType<P, R, E> =
//   | { type: "FETCH"; payload: P }
//   | { type: "REFETCH" }
//   | { type: "REVALIDATE" }
//   | { type: "RETRY" };

// const createMachine = 